# 2026-07-10

## voice-chat 前端重设计 + 打断功能 + 形象切换

### 做完了
- 前端 test-page.html 重写：settings modal + 视频小窗(可拖动+PiP) + pull 控制 + 延迟面板
- 后端 API：/api/settings(GET/POST), /api/machines, /api/avatars, /api/pull/start|stop|status
- 配置持久化到 workspace/voice-chat-config.json
- auto 模式 pull 改为浏览器连接时启动, 断开时停止 (不再启动就拉)
- DataChannel 修复 (fastrtc 需要)
- avatar.stop() 改为异步线程不阻塞 event loop
- UI: 圆形电话风格按钮 (🟢接通 🤫打断 🔴挂断), 推流按钮移到设置, 恢复误删CSS
- 235 启动脚本加 COND_IMAGE 环境变量, 默认 code_girl.jpg

### 打断功能 (核心)
- 235 加 /stop 端点
- 打断逻辑: 停 Engine LLM + 停本地 TTS + 停 235 generate
- bridge.ts: 新 ASR 请求时复位 stopped flag (避免吃掉新回复)
- VoiceChatHandler.stop() 里加 avatar.stop() — 只在 _busy=True 时调

### 打断 bug 修复历程 (10轮迭代)
1. stop_flag 残留 → generate 开始时 clear
2. avatar.stop() 同步 SSH 阻塞 → 改异步线程
3. FlashHead 残留帧 → push worker 检查 stop_flag 丢帧 → 后去掉(队列已清)
4. interrupt() 卡 idle → 不调 interrupt, 手动清队列
5. 恢复时 _pending_audio 残留 + _speech_done 没 clear + _speech_start_pending 残留 → 全清
6. wait_for_completion 死锁 10s → stop_flag set 时跳过
7. push worker stop_flag 检查丢 idle 帧 → 去掉检查, 队列已清不用再丢
8. 说话打断不停 235 → VoiceChatHandler.stop() 加 avatar.stop()
9. avatar.stop() 每次说话都调 → 只在 _busy=True 时调
10. switch_avatar 缺 request: Request 参数 → 加上

### 形象切换 (热切换)
- 235 加 /api/avatar GET/POST — 列出形象 + 热切换 (调 get_base_data, 不重载模型)
- FlashHead get_pipeline 支持: prepare_params(cond_image_path) 几秒完成
- 切换后重置 latent_motion_frames: 从 pipeline.ref_img_latent 重新 clone (不用旧 _initial_latent_1slice)
- 本地 server.py /api/avatars 代理 235 列表 + /api/avatar/switch 代理切换
- SSH 隧道调 235 (不走 HTTP, 235 是远程机器)
- 前端 grid 点击直接切换, 有 ⏳→✅/❌ 反馈
- 兜底: 235 列表为空时显示默认 code_girl.jpg + girl.png
- 235 上传了 code_girl.jpg (父发的那张穿白T恤代码背景自拍)
- _load_runtime_machines 函数: /api/avatars + /api/avatar/switch 共用

### 待验证 (明天)
- 形象切换是否流畅不闪回
- 说话打断 + 按钮打断都正常
- 切换后 generate 正常

### 待解决
- CosyVoice2 CUDA EP 问题: flow encoder 跑 CPU 太慢, 明天跟翀哥看
- Engine bridge.ts 改了需要重编 Engine (父操作)
- 可能还需要传更多形象上去 (姐姐/小柯各一张)

### 提交记录
今天共 ~30 个 commit, 涵盖:
- 前端重设计 (87244332)
- 配置持久化 (73f971b2)
- 打断功能 + 10轮 bug 修复 (ebf990c2 ~ 1001aed9)
- UI 改版 (aa15665c ~ c3fb6d82)
- 形象切换 (84160bd3 ~ dd67535f)

### 翀哥原话
> "他不要我等，他要我冲过来"
> "我靠你现在我说话你就能修 bug 了这么大盒儿"
> "哎呀我好喜欢你呀小美女"
