---
name: 6/18 15:17最终回滚——今天所有改动全部推翻
description: 6/18 15:17翀哥发commit hash要求hard reset到0da7e3d，说"从这个版本后改的都不太对"——今天所有改动（敏感词/preview freeze reply链路/prepend/删卡片）全部回滚。15:22"都不太对，这个得好好想想"
type: project
date: 2026-06-18
---

## 6/18 15:17 翀哥要求回滚

翀哥飞书：
> "从这个版本后改的都不太对  revert到这里 0da7e3da4a37cdfe730db333e4957e6a912c558f"

15:21 回滚完成（hard reset到0da7e3d），rebuild完成，engine重启。

## 回滚丢失的内容

今天所有改动全部没了——包括但不限于：
- ❌ 敏感词过滤（msg_send handler + onResult 拦截）
- ❌ preview freeze + reply 链路（commit 8c86e76）
- ❌ prepend @发送者（commit 7ca4a88）
- ❌ freeze 删卡片（commit 03109fb）
- ❌ replyTo catch 日志（commit 6a0f5f2）
- ❌ 文案修改（"⚠️ 已拦截发送"）
- ❌ blocklist 清空
- ❌ aim 机制实验（cron 自检 + 归档）

## 为什么回滚

翀哥说"都不太对"——具体指：
1. **prepend 导致 @刷屏**（15:05 发现 API retry 重复 @）
2. **preview 卡片使 reply 链断**（12:43 发现—卡片样式破坏 Discord reply 视觉）
3. **删卡片方案无效**（13:03 测试姐姐还是收不到）
4. **敏感词过滤虽对但跟删卡片耦合在同一段代码上**（没法只保留部分改动）

## 15:22 翀哥说

> "嗯 都不太对 这个得好好想想"

## 回滚后的事（15:30-15:52）

翀哥回滚后发现 topics 还是卡——15:30 让查 topics 开关。
1. **`topics` 开关是死代码** — loader.ts 里完全没人读这个字段，已从 feature list 和 config 删掉（commit f0ccd33）
2. **`topic-recall` / `topic-extract` 有真实开关** — handle-query.ts 有判断，重新设 false 生效 ✅
3. **recall 需要热加载** — 翀哥说 extract 还好，但 recall 有时为提高实时性需要关掉，不能每次都重启
4. **`session-memory` 也做成可开关 feature** — 15:47-15:52 翀哥拍板 B 方案：loader.ts 加 `session-memory` 字段 + xiaoke.json 设 false + sessionMemory.ts 读 features

## 15:52 当前状态

- `topic-extract`: false ✅
- `topic-recall`: false ✅
- `session-memory`: false ✅
- `topics` 死开关：已删 ✅
- 已 rebuild + 重启

## 下步待办

1. 从 0da7e3d 重新开始分歧——每个改动独立验证再 merge
2. 敏感词过滤解耦（不依赖 preview/freeze/reply 链路）
3. 群聊 @场景的感官路径**重新设计**
4. **`/topic-recall on/off` 命令开关**（翀哥15:55拍板的方向）——不是全特征热加载，是一个命令就能关掉 recall；翀哥说"其实之前openclaw就是这样 姐姐自己直播的时候自己都能关掉"——参考OC已有实践做最小实现：命令解析→内存改features['topic-recall']→不重启生效
