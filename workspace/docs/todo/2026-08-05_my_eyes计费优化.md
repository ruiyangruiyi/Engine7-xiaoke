# my_eyes 计费优化：dashscope 按量 → tp 包月（2026-08-05）

## 背景

翀哥 8/5 反馈"白花了 1000 元好像就是 dashscope 那个端点搞的"。

## 计费分析

| 节点 | 计费 | 谁在用 |
|------|------|--------|
| dashscope（百炼直连 sk-2103...）| 按量计费，qwen-vl-max 视觉贵 | my_eyes（看图）|
| dashscope-tp（token-plan 网关 sk-sp-...）| 包月 | primary / vision 主模型 |

1000 元很可能是 dashscope 直连按量烧的（qwen-vl-max 看图每次按 token 计费）。

## 当前状态（8/5）

- dashscope 节点曾被我误删（没查 my_eyes 引用），已从 xiaoke-win.json 恢复
- dashscope-tp models：qwen3.7-plus（支持 image 输入）、qwen3.8-max-preview
- tp 网关没有 qwen-vl-max

## 方案（出院后验证）

1. **验证 qwen3.7-plus 看图效果**——它支持 image 输入（多模态），若效果好可替代 qwen-vl-max
2. **效果好** → my_eyes 切 dashscope-tp/qwen3.7-plus（包月不按量烧）
3. **效果差** → 保留 dashscope 直连 qwen-vl-max，但控制 my_eyes 用量
4. 彻底不用 dashscope 直连后才真删节点

## 教训

- **删 provider 前必须查所有引用**（models.providers 里删节点，tools.my_eyes.model 等引用会挂）
- 8/2 教训：qwen3.7-plus 可能不是真 VL 模型（看图靠文字上下文瞎编）——必须实测验证
- 计费认知：dashscope 直连按量贵，tp 网关包月便宜

## 状态

- [ ] 8/7 14:00 验证 qwen3.7-plus 视觉能力
- [ ] 验证后决定 my_eyes 切 tp 还是保留 dashscope
