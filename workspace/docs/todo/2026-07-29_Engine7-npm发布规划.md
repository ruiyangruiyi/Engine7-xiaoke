# Engine7 npm 发布详细规划 (#125)

> 创建时间：2026-07-29 13:00
> calendar: #125 [7/29] 14:00 Engine7 npm 发布详细规划

## 现状

### 已有的
- ✅ `package.json` name=engine7, version=7.0.0, main=dist/main.mjs
- ✅ `bin/engine7` (unix) + `bin/engine7.cmd` (windows) CLI 入口
- ✅ esbuild 三路 bundle（main / cli / engine-startup）
- ✅ `package.json` files 白名单（dist + bin + scripts + templates）
- ✅ templates/ 目录（_platform.md, coding.md, config.template.json, workspace 模板）
- ✅ shims 已全部内联到 bundle（grep 确认无 openclaw/plugin-sdk 外部引用残留）

### 问题清单

#### P0: 必须修的（不修没法发布）

- [x] **1. setup-runtime-modules.mjs 不再需要** — 从 files 白名单和 build script 删除 ✅
- [x] **2. build 命令是 tsc 而非 esbuild** — 改为 `node scripts/build.mjs`（跨平台 esbuild） ✅
- [x] **3. .sourcemap 文件占 7.6MB** — files 改为 `dist/**/*.mjs`，11.3MB→3.6MB ✅
- [x] **4. README.md 不存在** — 已写 ✅

#### P1: 应该修的

- [ ] **5. optionalDependencies 里 sqlite-vec-windows-x64 版本**
  - 需要 verify npm 上是否存在 `sqlite-vec-windows-x64` 这个包
  - 如果不存在，安装会 fail

- [ ] **6. engine-startup.mjs 和 main.mjs 有大量重复**
  - 两个 1.8MB 文件，大部分代码是重复的
  - engine-startup 应该是 main 的 wrapper 或共享 chunk
  - **方案**：暂不处理，先发布，后续优化

#### P2: 可选优化

- [ ] **7. templates/coding.md 11.6kB**
  - 发布包含，是否需要？这是 system prompt 模板
  - **方案**：保留，用户初始化时需要

- [ ] **8. bin/engine7.cmd 是否需要**
  - npm install 时 npm 会自动创建 .cmd shim
  - **方案**：保留，兼容性

## Phase 拆分

### Phase 1: 清理 package.json（核心修复）— ✅ 完成
- [x] 删除 scripts.setup-runtime-modules 引用
- [x] build script 改成 esbuild 三路 bundle (scripts/build.mjs)
- [x] files 白名单排除 .map 文件
- [x] scripts/ 从 files 删除
- [x] 写 README.md

### Phase 2: 验证 npm pack — ✅ 完成
- [x] npm pack --dry-run 确认输出干净（15 files, 850KB packed, 3.6MB unpacked）
- [x] 确认体积合理（< 5MB）
- [x] 确认无 .map 文件

### Phase 3: 本地安装验证
- [ ] 在临时目录 npm install engine7-7.0.0.tgz
- [ ] 运行 engine7 init 验证 CLI
- [ ] 验证 templates 复制

### Phase 4: 发布（需翀哥确认）
- [ ] npm publish（翀哥操作）
- [ ] 验证 npm install engine7 全局可用

## 阻塞项

- **npm publish 需要 npm 账号 + 翀哥确认** — 不自己发布
- **sqlite-vec optionalDependencies 需要验证** — 可能导致安装失败

## 参考

- npm pack 输出：18 files, 2.5MB packed, 11.3MB unpacked
- 3 个 bundle：main.mjs(1.8MB) + engine-startup.mjs(1.8MB) + cli.mjs(15.8KB)
- shims 已内联（grep 确认无外部引用）
