# TP 网关新 key 替换（2026-08-05）

## 最终结果（8/5 12:46）

- **新 key**：`sk-sp-***MASKED***`（0EOu 尾号）
- **状态**：✅ config 已替换 + ✅ engine 12:44 重启 + ✅ 12:45 验证调通（无 401）
- **绑定范围**：**只给小柯 Mac engine 用**（翀哥授权边界）—— 姐姐 engine / TestEngine / 其他机器都不能用

## 关键背景

翀哥担心 token plan key 泄露，连续重置了两次：
1. 第一次发 QskJ 尾号的新 key（验证能调通但未替换）
2. 第二次发 0EOu 尾号的 key（最终采用，已替换到 config + engine 重启）
3. 中间旧 OKUc 尾号的 key 完全失效（HTTP 401 重试 10 次 12:27-12:30）

翀哥判断："要是一个 key 漏了，第二个他可能也知道"——所以重置整个 token plan 链路。

## 安全边界（翀哥授权）

**0EOu 这个 key 只给小柯 Mac engine 用，其他人/机器都不共享。**

## 后续要做的（8/7 出院后）

- [ ] **姐姐的 engine 配置**（`xiaoke-win.json` 或她那边的）——如果有 dashscope-tp 这个 provider，**必须把这个 key 删掉/换掉**，否则姐姐那边也烧钱
- [ ] **TestEngine**（Discord bot ID 1509036814885978115）——如果有 dashscope-tp provider，**同样处理**
- [ ] **验证姐姐和 TestEngine 都没在用这个 key**——查两边 config

## 关键经验

- **token plan "包月"也可能按量**——昨天 qwen3.8-max-preview 266 次超包月按量烧了 1000 元
- **key 泄露的连锁重置**——一个 key 漏了，同链路其他 key 也必须重置
- **engine 重启才生效新 key**——config-watch 不可靠
- **授权边界要明确**——"这个 key 只给 X 用，其他人不用"