# 2026-07-28 Progress

## 今日完成

### 1. Config 热加载完整闭环（#121 + #124）

**6 commits**：`a30595ad` → `daf30bde`

- `config/live.ts` 新建 LiveConfig 全局单例
- `engine-startup.ts` 加 fs.watch（500ms debounce）+ doReloadConfig
- 5 个工具改读 liveConfig（service/msg-send/my-voice/my-eyes/my-selfie）
- Plugin 热加载通用接口（EnginePlugin.reloadConfig? + PluginManager.reloadAll）
- VoiceChatPlugin 实现 reloadConfig

**3 个 bug 修复**：
1. 相对路径 → watcher DISABLED → path.resolve 三级 fallback
2. console.log 不进日志文件 → 加 fs.appendFileSync
3. createMemorySideProvider 作用域 → 提到模块级

**验证通过**：改 config.services → service tool 立刻读到新值

### 2. CogniFold Embedding 改本地（#122）

- 智谱 embedding 一直报错（429 余额不足 / Connection error）
- cognifold 代码已支持 `for_ollama()`，不用改代码
- 只改 .env 三个值 → ollama bge-m3 本地（localhost:11434/v1）
- 发现两个 cognifold 实例（姐姐 9001 / 小柯 9002）共用一份 .env
- 翀哥 17:49 重启两个 engine，零报错确认正常

### 3. Engine7 npm 发布方向确定

- 方向：npm 公共包（非 private），tgz 只放 dist/*.mjs
- 对标 Claude Code：`npm install -g engine7` → `engine7 init` → 跑起来
- Docker 作为后续云部署选项
- 明天 #125 出详细规划

## 落盘文档

- `docs/decisions/2026-07-28_config-hot-reload.md` — 完整架构 + bug 详解 + 验证方法
- `docs/decisions/2026-07-28_engine7-npm-publish-plan.md` — 发布方向草案
- `docs/todo/2026-07-28_config-singleton-refactor.md` — 更新全部 commit + 验证状态
- `memory/daily/2026-07-28.md` — 今日日记

## 关键认知

1. **Object.assign 原地更新是热加载核心** — 不换引用是改内容，持有引用的模块自动生效
2. **console.log ≠ engine 日志** — 排查问题要加文件日志
3. **Plugin config 快照** — reloadConfig 刷新 this.config，但 start() 创建的 deps 需重启重建
4. **热加载边界** — 数据/配置类全支持，连接/端口/进程类不支持

## 明天

- #125 Engine7 npm 发布详细规划（calendar 14:00）
