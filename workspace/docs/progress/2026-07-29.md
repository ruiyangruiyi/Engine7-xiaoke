# 2026-07-29 Progress

## 🎯 今日核心：engine7 上 npm + CC header 伪装

从 GLM 封号事件出发，一天内完成了 CC header 三层伪装 → engine7 npm 发布 → Mac 跨平台验证。

## 完成事项

### CC Header 三层伪装（commit 72ce95d1）
- **背景**：智谱检测到 engine 的 header 不像 CC，封了一个月不退钱，12345 投诉无果
- **方案**：翀哥让 CC 自己从 v2.1.220 二进制提取完整 header 结构
- **三层**：
  - ① SDK 指纹：X-Stainless 全家桶（Lang/Package-Version/OS/Arch/Runtime/Runtime-Version/Retry-Count/Timeout）
  - ② CC 身份：User-Agent claude-cli/2.1.220 + x-app:cli + X-Claude-Code-Session-Id
  - ③ 认证+beta：Authorization Bearer + anthropic-beta 特性开关

### engine7 npm 发布（7.0.0 → 7.0.3）
- **7.0.0**：首次发布，850KB，15 files
  - build.mjs 跨平台 esbuild 脚本
  - files 白名单排除 .map（11.3MB→3.6MB）
  - README.md
  - sqlite-vec optionalDependencies 验证通过
- **7.0.1**：
  - start.sh/start.cmd 跨平台启动脚本
  - 全局/本地路径自动检测
  - contacts.md 脱敏（删除真实 ID）
  - 私有工具开关（msg_husband / wx_query 默认不暴露）
  - autoDream 硬编码路径修复 + dlog 改 console.log
- **7.0.2**：
  - engine7 start 子命令（一行启动 + 杀旧进程）
  - 预装 6 skills（docx/pptx/xlsx/pdf/sop/aim）
  - init 时 vision 模型自动检测
- **7.0.3**：
  - config 模板 order 调优（contacts.md + distill-output.md + MEMORY.md）
  - init 加 Tavily API Key 可选配置

### Mac 跨平台验证
- 14 年 Mac Pro（Intel x64），nvm 装 Node 22
- npm install -g engine7 成功（160 packages）
- engine7 init --quick --dry-run 正常
- engine7 start 成功拉起，飞书消息收发正常
- vision + my_eyes 验证通过（翀哥发睿阳小时候照片测试）
- 私有工具隔离验证通过（xiaowen 不再乱叫老公）

### USER.md 整理
- 从 21 行 3640 字节剪到 35 行 581 字节
- 删掉过时信息（小欧已关闭、姐姐叫妹妹）
- 新增 GLM 封号、8/4 住院、通信偏好等

## 发现的问题（明天修）

### MEMORY.md 双重注入（#127）
- `workspace/MEMORY.md`（short-term-promotion 写）和 `topics/MEMORY.md`（auto memory extract 写）内容重叠
- system prompt 里注入了两份，内容不完全一样但大部分重复
- 根因：`buildMemoryPrompt` 读 `topics/MEMORY.md` 内容 + staticFiles 读 `workspace/MEMORY.md`

## 统计

- **Commits**：~25 个
- **npm 版本**：4 个（7.0.0 → 7.0.3）
- **平台验证**：Windows + macOS
- **工作时间**：09:00 → 21:40（约 12 小时）

## 明日计划

- #126 10:00 engine7 开机自启动
- #127 11:00 MEMORY.md 双重注入修复
- 发 7.0.4（攒 tavily + #126 + #127）
