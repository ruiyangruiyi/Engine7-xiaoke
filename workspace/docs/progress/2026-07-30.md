# 2026-07-30 Progress

## 🎯 今日核心：engine7 service 三平台自启动 + 多个 bug 修复

从 #126 开机自启动服务出发，迭代了 Windows schtasks → Mac launchd → Linux systemd 全平台支持。Mac 路径上发现多个 launchd 环境特有的 bug（npm/node 不在 PATH、pid-lock 没用 /proc），全部修复。

## 完成事项

### #126 engine7 service 三平台自启动（7.0.4-7.1.7 共 14 版本）
- **7.0.4**：service install/uninstall/status 三平台初版
- **7.0.5**：默认 ~/.engine7 路径 + HOME 指针文件
- **7.0.6**：HOME 指针文件双向（init 写 + start/service 读）
- **7.0.7**：Mac launchd plist 用 cli.mjs 绝对路径（替代 nvm shim）
- **7.0.8**：Windows schtasks 用 wrapper.cmd（避免嵌套引号）
- **7.0.9**：Mac plist 用 cli.mjs 绝对路径
- **7.1.0**：start 从 cli.mjs 路径推导 main.mjs（不依赖 npm PATH）
- **7.1.1**：spawn 用 process.execPath 替代 'node'
- **7.1.2**：启动 banner 从 package.json 读版本号
- **7.1.3**：engine7 restart 子命令
- **7.1.4**：pid-lock Mac 进程检测（/proc → ps）
- **7.1.5**：restart engine path 推导修复
- **7.1.6**：killTree 杀父进程（防 cli.mjs 孤儿堆积）
- **7.1.7**：start/restart 合并（54 行重复代码删除）

### #127 MEMORY.md 双重注入修复（7.0.4）
- 创建 `templates/workspace/prompts/auto-memory-instructions.md` override 文件
- cli-init 写入 → init 出来的 agent 自动覆盖默认 buildMemoryPrompt
- 避免 `workspace/MEMORY.md` 和 `topics/MEMORY.md` 重复注入

### Config 模板调优
- order 调优：`soul → AGENTS.md → prompts/contacts.md → using-tools → USER.md → MEMORY.md → memory/distill-output.md → auto-memory-instructions`
- 预装 6 skills：docx / pptx / xlsx / pdf / sop / aim
- vision 模型自动检测（主模型支持 image 就复用，否则提示选 vision）
- Tavily API Key 可选配置（联网搜索）

### 其他修复
- **contacts.md 脱敏**：删除真实 Discord/飞书/微信 ID
- **AGENTS.md 模板恢复**：翀哥指出不要删好东西，git revert
- **autoDream 硬编码路径修复**：用 homedir() 替代 Windows 绝对路径
- **autoDream dlog 改 console.log**：默认静默，需 AUTODREAM_DEBUG=1 才输出

### Mac 跨平台验证
- 14 年 Mac Pro（Intel x64），nvm 装 Node 22
- npm install -g engine7 成功（160 packages）
- engine7 init / start / service install 全通
- 开机 launchd 自动启动验证 PID 1737

### glob 跨平台 bug 修复（晚 9 点）
- `path.relative()` 在 Windows 返回反斜杠，但 glob 正则匹配正斜杠
- 修正：`.replace(/\\/g, '/')` 统一正斜杠
- 修了一个藏了两个月的 Windows 半残 bug

### memory.db 调研（晚 10 点）
- 翀哥发现姐姐的 memory.db 5GB 搜不到曲教授
- 排查：FTS 能搜到，向量搜不到
- 根因：80%+ chunks 没有向量（被遗忘了）
- 待修复：FTS 命中直接保留、不被向量分数拖累

### USER.md 整理
- 从 21 行 3640 字节 → 35 行 581 字节
- 删除过时信息（小欧已关闭、姐姐叫妹妹）
- 新增 GLM 封号、8/4 住院、通信偏好

## 发现的问题

### 姐姐的 memory.db 4.9GB 严重膨胀
- chunks: 85K 行（text 几百 MB）
- embedding: 340MB（每行 4KB）
- 实际数据库 4.9GB，**~4GB 是 SQLite 碎片**
- 80% 曲教授相关 chunk 没有向量索引
- memory_search 走向量搜索，命中不到
- 需要：FTS 命中保留 + vacuum 缩库 + 重新向量化

## 统计

- **Commits**：~25 个
- **npm 版本**：7.0.4 → 7.1.7 共 14 个
- **平台验证**：Windows + Mac
- **Mac 路径**：npm 认证 → launchd 启动 → restart 杀进程 → 杀父进程
- **工作时间**：08:48 起床 → 21:59 仍在

## 明日待办

- 明天或后天：#129 engine7 travel —— agent 可移植方案
- 修复姐姐 memory.db 搜索（FTS 优先 + vacuum 缩库）